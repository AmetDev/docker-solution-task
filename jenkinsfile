pipeline {
  agent {
   dockerfile true
  }
  stages {
    stage('Test') {
      steps {
        sh 'nginx -v'
      }
		}
		stage('Start container') {
      steps {
        sh 'docker-compose up -d'
        sh 'docker-compose ps'
      }
    }

  }

}
